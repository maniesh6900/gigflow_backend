```
    create a env file and add dataBase url,
```

To create an `.env` file and add a database URL, follow these steps:

### 1. Create the `.env` File

<!-- make sure it is in the root directory of your project -->
